# Dependencies
node_modules/
sigma_studio/node_modules/

# Build output
sigma_studio/dist/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment / Secrets
.env
.env.local
.env.*.local
*.env

# Config with API keys
config.json
config.local.json

# Data modules (each user adds their own)
data/
!data/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Debug / Temp test output
test_1M_output.txt
test_output.txt
*.tmp

# Scratch (temporary workspace)
scratch/
!scratch/.gitkeep

# Pipeline definitions (user-specific)
collatz_pipeline.json

# Virtual Environment
.venv/
projects/
sandboxes.json

# OS files
.DS_Store
Thumbs.db

# Database files (contain user data)
agent_context.db
agent_memory/

# Research sessions (user-specific)
research_sessions/

# Private/Personal manifests & training data (not tracked in git)
manifesti/Private/
!manifesti/Private/.gitkeep
training/Private/*
!training/Private/.gitkeep
!training/Private/*/.gitkeep

# Agent metadata (auto-generated from manifesti + API)
agents_meta.json

# Temp / generated files
sigma_studio/build_log.txt
sigma_studio/modules_meta.json
modules_meta.json
tasks.json

# Training job outputs (personal/study specific)
training/jobs/*
!training/jobs/.gitkeep

# Temporary/downloaded user test datasets
training/datasets/hf_*
training/datasets/local_*
!training/datasets/.gitkeep

# Training python cache
training/__pycache__/
training/*/__pycache__/
!training/.gitkeep
!training/scripts/.gitkeep

# Unsloth compiled cache (auto-generated, user-specific)
unsloth_compiled_cache/

# Notebooks / experiments
*.ipynb
sigma_studio/.build_stamp
.backups/*
.training/*
