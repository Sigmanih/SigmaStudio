[package]
name = "sigma-model"
version = "0.1.0"
edition = "2021"
authors = ["Sigma Studio Team <dev@sigmastudio.local>"]
description = "GGUF/SafeTensors reader ed Execution Planner hardware-aware per SigmaEngine"

[dependencies]
sigma-core = { path = "../sigma-core" }
sigma-memory = { path = "../sigma-memory" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
