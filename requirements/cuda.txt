# ==============================================================================
# Sigma Studio — CUDA Dependencies (NVIDIA GPU)
# Install with: pip install -r requirements/cuda.txt
# For specific CUDA version: pip install torch --index-url https://download.pytorch.org/whl/cu124
# ==============================================================================
-r ai-core.txt
torch>=2.0.0
peft>=0.10.0
bitsandbytes>=0.43.0

# GGUF runtime (llama-cpp-python) is not listed here: its source differs per
# accelerator. sigma_launcher.py installs it automatically at first launch;
# to do it by hand see requirements/inference.txt.
