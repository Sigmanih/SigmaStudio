# ==============================================================================
# Sigma Studio — AI Core Dependencies (CPU-compatible)
# ==============================================================================
-r base.txt
# Legge i checkpoint. Il minimo non e' un numero prudente: e' la versione in
# cui esistono le architetture che il Model Hub scarica oggi. Con una piu'
# vecchia il download riesce, il caricamento no, e l'errore parla di
# "architettura non riconosciuta" — che si legge come un checkpoint rotto e non
# come una libreria da aggiornare. Gemma 4 (gemma4_unified) richiede la 5.10.
transformers>=5.12.0
datasets>=2.18.0
huggingface_hub>=0.22.0
safetensors>=0.4.0
accelerate>=0.28.0
gguf>=0.1.0
